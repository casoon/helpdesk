# pnpm 11 blocks packages younger than 24h by default.
# Disabled here because this repo actively updates dependencies and sources them
# from well-known registries (npm public registry only).
minimum-release-age=0
